<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.GuildChatMessageMapper">

    <resultMap type="com.ruoyi.system.domain.GuildChatMessage" id="Result">
        <id property="id" column="id"/>
        <result property="guildId" column="guild_id"/>
        <result property="senderId" column="sender_id"/>
        <result property="senderName" column="sender_name"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectHistory" resultMap="Result">
        SELECT * FROM guild_chat_message
        WHERE guild_id = #{guildId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectSinceId" resultMap="Result">
        SELECT * FROM guild_chat_message
        WHERE guild_id = #{guildId} AND id > #{sinceId}
        ORDER BY id ASC
        LIMIT 100
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO guild_chat_message (guild_id, sender_id, sender_name, content, type, created_at)
        VALUES (#{guildId}, #{senderId}, #{senderName}, #{content}, #{type}, NOW())
    </insert>

</mapper>
